pip install flask, flask_postgresql, flask_wtf,
psycopg2, email_validator, itsdangerous==2.0.1 (compatible version),
flask_bootstrap

db and mail info are stored in env variables, I can share them if requested.
